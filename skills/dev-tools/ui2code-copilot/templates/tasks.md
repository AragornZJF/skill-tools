# 任务拆分 — 需求名称

> 拆分顺序：类型定义 → 接口层 → Hooks/逻辑层 → 组件层 → 页面/路由
> 每个任务 = 可独立提交的原子变更（3-5 个文件）
> 每个任务必须精确到文件路径和组件/函数签名

## 前置条件

- [ ] （依赖/配置等前提）

## Task 1: 任务名

- **目标**: 一句话描述
- **涉及文件**:
  - path/to/Component.tsx — 新增/修改，做什么
- **关键签名**:
  ```typescript
  const ComponentName: React.FC<Props> = ({ prop1, prop2 }) => { }
  ```
- **依赖**: 无
- **验收标准**: 怎样算完成
- **验证命令**（可选）:
- [ ] 完成

## 变更摘要

- 总文件数: X 个文件
- 遗留问题:
